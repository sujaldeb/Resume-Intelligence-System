# CPU-only PyTorch build — sentence-transformers pulls in torch as a
# dependency; without this index, pip installs the CUDA build by default,
# which is several GB larger and unnecessary on CPU-only deployment targets
# (Streamlit Cloud, HF Spaces, Render).
--extra-index-url https://download.pytorch.org/whl/cpu

streamlit==1.57.0
pandas==2.3.3
numpy==2.4.4
pdfplumber==0.11.9
python-docx==1.2.0
sentence-transformers==5.5.1
transformers==5.9.0
torch==2.11.0
google-generativeai==0.8.6
